import {Text, View} from "react-native";
import { indexStyle } from "@/src/styles/indexStyle";

export default function Cadastro() {
  return (
    <View style={indexStyle.container}>
      <Text>Aqui vai ser a pagina de cadastro =D </Text>
    </View>
  );
}